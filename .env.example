POSTGRES_DB=bibliotheque_db
POSTGRES_USER=biblio_user
POSTGRES_PASSWORD=biblio_password

# Utilises par docker-compose.jenkins.yml (voir README.md, section
# "Pipeline CI/CD") pour creer le compte admin via Configuration as Code,
# sans passer par l'assistant de configuration initiale de Jenkins.
JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=changeme
